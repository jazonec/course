replicaCount: 1

image_bot:
  pullPolicy: Always
  repository: ghcr.io/mossad-zika/bitrix_24_salesman_gpt-telegram_bot
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

image_liquibase:
  repository: ghcr.io/mossad-zika/bitrix_24_salesman_gpt-liquibase
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

secret:
  name: app-secrets
